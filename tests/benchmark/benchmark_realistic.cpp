// Replace the overly optimistic benchmark
void benchmark_with_real_motion() {
    // Create frames with increasing motion
    for (int motion_pixels = 5; motion_pixels <= 50; motion_pixels += 5) {
        // Test tracking with this amount of motion
        // Report where tracking breaks down
    }
}