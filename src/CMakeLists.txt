# For now, just create a simple main executable
add_executable(ar_slam_app main.cpp)

# We'll add libraries later as we create the files
# add_library(slam_core SHARED
#     core/memory_pool.cpp
#     core/frame.cpp
# )